[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn main_v25:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
# Force rebuild Sat, Feb 01, 2026 10:20:00 PM - added /sms endpoint
